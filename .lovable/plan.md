# Rename "What to Wear" → "Dripd Calendar" in the homepage

## Changes

**File: `src/pages/HomeScreen.tsx**`

- Find all occurrences of "What to Wear" (headings, comments, labels) and replace with "Dripd Calendar"

Single file, text replacement only.