

## Plan: Change Cultural Icon to Red Flag

### Change (`src/pages/HomeScreen.tsx`)
- Replace `Landmark` with `Flag` from `lucide-react` for the "Cultural" category
- Update the import statement accordingly

### Single line change
- `src/pages/HomeScreen.tsx` — swap `Landmark` → `Flag` in import and in the occasions array

